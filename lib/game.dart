import 'package:flame/game.dart';
import 'dart:ui';

class FlameShooterGame extends Game {
  Size screenSize;
  FlameShooterGame(this.screenSize);

  @override
  void update(double dt) {
  }

  @override
  void render(Canvas canvas) {
  }
}
